name = "chatbot-api"
main = "src/index.js"
compatibility_date = "2024-01-15"
compatibility_flags = ["nodejs_compat"]

[dev]
port = 8787

[env.production]
name = "chatbot-api-prod"

[vars]
NODE_ENV = "production"
# Secrets are set via: wrangler secret put <KEY_NAME>
# SUPABASE_URL and SUPABASE_KEY are stored as secrets